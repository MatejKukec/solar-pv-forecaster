# Project Issue Tracker

## Open Issues
| # | Date | Issue | Attempt | Status | Notes |
|---|------|-------|---------|--------|-------|

## Resolved Issues
| # | Date Opened | Date Resolved | Issue | Fix Applied | Confirmed |
|---|-------------|---------------|-------|-------------|-----------|
| 1 | 2026-07-09 | 2026-07-09 | `httpx.Timeout(connect=5.0, read=20.0)` raised `ValueError` — httpx requires a default alongside partial overrides | Added explicit default: `httpx.Timeout(10.0, connect=5.0, read=20.0)` in all three data-ingestion clients | ✅ (tests pass) |
| 2 | 2026-07-09 | 2026-07-09 | `pvlib.pvsystem.pvwatts_ac` no longer exists in pvlib 0.15 — moved to `pvlib.inverter.pvwatts` | Updated `physics_engine/pv_model.py` to call `pvlib.inverter.pvwatts` | ✅ (tests pass) |
| 3 | 2026-07-09 | 2026-07-09 | `mypy --strict` failures: untyped pvlib returns, loose `dict` type args, `httpx` params typing, `Hashable` index from `iterrows()` | Added `pvlib.*` mypy override, explicit `dict[str, Any]`/`dict[str, str \| int \| float]` annotations, `cast()` at the two genuinely-untyped pvlib call sites | ✅ (`mypy src/` clean) |
| 4 | 2026-07-09 | 2026-07-09 | `recharts@2.x` is deprecated (no longer actively maintained per npm) | Upgraded to `recharts@^3` | ✅ (build passes) |
| 6 | 2026-07-09 | 2026-07-09 | `npm install` flagged 2 vulnerabilities: esbuild (moderate, dev-server-only) and Vite path traversal `GHSA-4w7w-66w2-5vf9` (high, CVE-2026-39365) — entire Vite 5.x line is affected, fix requires ≥6.4.2 | Upgraded `vite` 5.4.21 → ^7.3.6 and `@vitejs/plugin-react` → ^4.7.0 (paired versions, avoids the breaking jump straight to Vite 8 that `npm audit fix --force` wanted). Verified typecheck, build, and dev server all still work | ✅ (`npm audit`: 0 vulnerabilities) |
| 7 | 2026-07-09 | 2026-07-09 | (Day 2) `mypy --strict` flagged `pandas` as missing stubs, pre-existing from Day 1 | Added `pandas-stubs` to backend dev dependencies | ✅ (`mypy src/` clean) |
| 8 | 2026-07-09 | 2026-07-09 | (Day 2) `POST /api/calibration/production` raised `sqlalchemy.exc.StatementError` — `ProductionLog.production_date` (a `date` column) was assigned the raw ISO string from the request body instead of a parsed `date` | Parse with `date.fromisoformat(entry.date)` before constructing the row | ✅ (smoke-tested end to end with mocked HTTP) |
| 9 | 2026-07-09 | 2026-07-09 | (Day 4) Adding auto-seeded demo data at the frontend's default coordinates collided with `conftest.py`'s `sample_location`/test fixtures using the same coordinates — tests became order-dependent and one failed on unmocked `get_historical` calls triggered by the seeded history | Gave each test a fresh, isolated SQLite DB via `dependency_overrides[get_session]` in the `client` fixture (see `conftest.py`), instead of sharing the app's real (seeded) database | ✅ (`pytest`: 57/57, order-independent) |
| 10 | 2026-07-09 | 2026-07-09 | (Day 5) `npm run lint` had existed since Day 1 but `eslint` was never installed as a dependency — script failed with `eslint: not found` | Installed `eslint@^10`, `typescript-eslint@^8`, `eslint-plugin-react-hooks@^7`, `eslint-plugin-react-refresh@^0.5`, `globals@^17`, `@eslint/js`, and added a flat `eslint.config.js` | ✅ (`npm run lint`: 0 errors, `npm audit`: 0 vulnerabilities) |
| 11 | 2026-07-11 | 2026-07-11 | (Pre-publish audit) 8 backend files + 1 frontend file had CRLF line endings (missing trailing newline, `ruff` W292) while the rest of the repo is LF — leftover from edits made in a Windows editor on Days 3-5. A stray, empty, malformed directory named `{config,api` also existed under `backend/src/backend/`, from a failed shell brace-expansion `mkdir` | Normalized the 9 files to LF, ran `ruff check --fix` + `ruff format`, deleted the empty stray directory | ✅ (`ruff check`/`ruff format --check`: clean, `pytest`: 76/76, `eslint`/`tsc -b`: clean) |
| 12 | 2026-07-11 | 2026-07-11 | (Pre-publish audit) `GEMINI_MODEL` default in `config.py` and `render.yaml` was `gemini-flash` — not a valid Gemini API model ID (the bare alias needs a `-latest` suffix or a real version) — while `.env`/`.env.example` correctly used `gemini-3.5-flash`. Only local dev (which reads `.env`) worked; a Render deploy with `GEMINI_API_KEY` set would have gotten a 502 on every AI request | Aligned `config.py`'s default and `render.yaml` to `gemini-3.5-flash`, matching `.env.example` | ✅ (`pytest`: 76/76, `mypy --strict` clean) |
| 13 | 2026-07-11 | 2026-07-11 | (Pre-publish audit) Docs had drifted from the code: root/backend READMEs said "66 tests" (actual: 76, grew across Days 2-5) and claimed the NASA POWER client was "tested against mocked HTTP responses" (it has 0% coverage and no route calls it — `vulture`/coverage report confirmed). Root/frontend READMEs also listed component names (`LocationForm`, `SummaryCard`, `ChatWidget`) that don't exist — renamed to `SettingsPage`/`StatusBar`/`AiAssistantPanel` at some point without a doc update | Corrected test/coverage counts and component names in all three READMEs; reworded the NASA POWER and AI-anomaly-flagging claims to state plainly what's implemented vs. wired in, matching the project's existing CAMS disclosure pattern | ✅ (cross-checked against actual file tree, `pytest --cov`, and `grep`) |
| 14 | 2026-07-11 | 2026-07-11 | (Pre-publish audit) `.vs/` (Visual Studio's per-machine solution cache, including a `.sqlite` workspace DB) wasn't in `.gitignore` — `git add .` on Windows with the solution open would have committed it | Added `.vs/` to `.gitignore` | ✅ (verified pattern matches the folder in the zipped project) |
